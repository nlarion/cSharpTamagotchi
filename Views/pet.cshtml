<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>@Model.GetName()</h1>
    <ul>
      <li>Current Metal Level for @Model.GetName(): @Model.GetFood()</li>
      <li>Current Attention for Level @Model.GetName(): @Model.GetAttention()</li>
      <li>Current Rest Level for @Model.GetName(): @Model.GetRest()</li>
    </ul>
    <a href="/">Add another たまごっち</a>
    <br>
    <form action="/pets/@Model.GetId()/food" method="post">
      <button type="submit">Feed  @Model.GetName() tin cans</button>
    </form>

    <form action="/pets/@Model.GetId()/attention" method="post">
      <button type="submit">Give  @Model.GetName() some attention</button>
    </form>

    <form action="/pets/@Model.GetId()/rest" method="post">
      <button type="submit">Let  @Model.GetName() rest up</button>
    </form>

  </body>
</html>
